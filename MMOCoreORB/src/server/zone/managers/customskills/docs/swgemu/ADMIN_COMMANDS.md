# SWGEmu Admin (GM) Commands — Operator Reference
*Audience: server operators / GMs.* **133 admin commands** on this build — every entry parsed directly from its C++ command class with file:line cites. Code is authoritative; the owner's Commands V2 PDF served as cross-reference only. Permission background: `CODE_REFERENCE.md` → Gating chain.

## Client prerequisite — user.cfg (required)
Admin commands are **blocked client-side** until enabled: without this, the client refuses to send them and returns "no such command" even with server god mode active. In the **client's** SWG game folder, create/edit `user.cfg`. Working reference (operator-provided, verified in use):
```
[SwgClient]
allowMultipleInstances=true

[ClientGame]
0fd345d9 = true

[ClientUserInterface]
debugExamine=0

[ClientGraphics]
useSafeRenderer=0
rasterMajor=7
constrainMouseCursorToWindow=false
```
`[ClientGame] 0fd345d9 = true` is the required unlock (the TRE files gate features on it); everything else in this file is optional preference. Commonly added admin QoL keys under `[ClientUserInterface]`: `debugExamine=1`, `allowTargetAnything=1`, `drawNetworkIds=1`. Gotchas: if `user.cfg` doesn't exist, copy `swgemu.cfg` and replace contents; ensure `swgemu.cfg` contains `.include "user.cfg"` (often missing/commented); fully restart the client after edits.

## Permission model
Every admin command requires BOTH on the executing character: (1) **God mode** (`ghost->hasGodMode()`) — account-level admin privilege; (2) **Command ability** (`ghost->hasAbility("/cmd")`) — character must hold the ability like any skill. Either fails → `@error_message:insufficient_permissions` + `adminLog` warning. All successful calls logged via `logAdminCommand(...)`. Boot inventory of restricted commands: set `Core3.CommandConfigManager.DumpAdminCommands = true`. Syntax: `<required>`, `[optional]`.

### Admin levels (/setGodMode values)
15 Administrator · 14 Developer · 13 Quality Assurance (QA) · 12 Community Support Rep (CSR) · 11 Event Coordinator (EC) · 10 EC Intern · 9 Community Support Intern · 8 CT · 7 CC · 6 Tester · 1 Intern · 0 non-privileged player (default). Server side, an account's `admin_level` (DB) or `adminGodToAll=1` in `localOptions.cfg [GameServer]` governs login god access; `/setGodMode <level|on|off>` then sets the character's level in-game (`Account.idl:24`; `CommandConfigManager.h:112` defines GODLEVEL=72 internally).
## Account Control
/removeAllowedPlayer [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /removeAllowedPlayer | File: RemoveAllowedPlayerCommand.h:16  
/activateQuest <quest> -- Activates the specified quest on the targeted player creature's PlayerObject | Example: /activateQuest 15 | File: ActivateQuestCommand.h:49  
/addAllowedPlayer [no args] -- Empty stub that only performs state-mask and locomotion checks and returns success without touching arguments or targets | Example: /addAllowedPlayer | File: AddAllowedPlayerCommand.h:16  
/addBannedPlayer [<player>] <duration> [<message>] -- Bans the named player (or the current target if no name given) via their account for the given integer duration with the remaining tokens joined as the ban reason | Example: /addBannedPlayer JohnDoe 3600 exploiting bugs repeatedly | File: AddBannedPlayerCommand.h:41  
/removeBannedPlayer [<player>] <reason> -- Looks up the named player (or uses the targeted object if no name token is given), verifies their account has a ban entry on this galaxy, and unbans the character recording the admin-supplied audit reason appended to the original ban record | Example: /removeBannedPlayer JohnDoe ban lifted after appeal review | File: RemoveBannedPlayerCommand.h:37  
/deactivateQuest <questName> -- Privileged command that clears the journal quest whose name hash matches the given quest name from the currently targeted player creature | Example: /deactivateQuest quest_c_dathomir_gall | File: DeactivateQuestCommand.h:49  
/emptyMailTarget [<player>] -- Deletes every persistent mail message from the mailbox of the named player looked up by first name or, if no name given, the caller's currently targeted player (never the caller themselves) | Example: /emptyMailTarget JaneDoe | File: EmptyMailTargetCommand.h:29  
/freezePlayer <player> [<reason>] -- Freezes and mutes the current player target or the named player (roots them for 3600s, zeroes speed, sets FROZEN state, mutes chat) recording the remaining tokens as mute reason | Example: /freezePlayer john exploiting resources | File: FreezePlayerCommand.h:42  
/unfreezePlayer [<player>] -- Removes the FROZEN state buff/state and speed penalty plus clears mute flag and reason from the targeted player creature, or the player looked up by first name if nothing valid is targeted | Example: /unfreezePlayer HanSolo | File: UnfreezePlayerCommand.h:42  
/getAccountInfo <-c|-a|-i> <name> -- Opens the account-info SUI session for the admin showing username, IDs, ban status, characters and IPs, looking up by character first name (-c), account name or ID (-a, extra tokens joined), or online IP address (-i) | Example: /getAccountInfo -c luke | File: GetAccountInfoCommand.h:52  
/getPlayerId [<player>] -- Sends the caller a system message with the objectID of the named player or, if no token given, the current creature target | Example: /getPlayerId luke | File: GetPlayerIdCommand.h:32  
/getStationName [<player>] -- Sends the caller the account (station) username of the named player or, if no token given, the current creature target | Example: /getStationName luke | File: GetStationNameCommand.h:36  
/kick <firstName> <reason> -- Disconnects the targeted player or the named player from the server, requiring the remaining tokens as the kick reason | Example: /kick BobPlayer repeated griefing | File: KickCommand.h:39  
/killPlayer [<playerName>] [-area [range]] [-wounds] [<health> [action] [mind]] -- Kills the targeted/named player or pet, or all players and pets within an optional radius, optionally applying wounds and/or custom damage amounts instead of instant death | Example: /killPlayer -wounds | File: KillPlayerCommand.h:70  
/setFirstName [--force|-f] <newFirstName> -- Changes the targeted player's first name, optionally skipping name verification when an adminLevel >= 15 caller passes the --force or -f flag | Example: /setFirstName -f Luke | File: SetFirstNameCommand.h:45  
/setLastName [--force|-f] <newLastName> -- Changes the targeted player's last name, optionally skipping name verification when an adminLevel >= 15 caller passes the --force or -f flag | Example: /setLastName Skywalker | File: SetLastNameCommand.h:45  
/unCityBan [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without lifting any city ban | Example: /unCityBan | File: UnCityBanCommand.h:16  
/wipeItems [<player>] -- Destroys every object in the targeted player creature's inventory (or the online player looked up by first-name token) both from the world and database | Example: /wipeItems JarJarBinks | File: WipeItemsCommand.h:42  

## Server Control
/broadcast [-event|-imperial|-rebel] [<message>] -- Sends a system broadcast galaxy-wide, where -event/-e prefixes an orange [Event] tag, -imperial/-i sends only to Imperial players, -rebel/-r only to Rebel players, otherwise the raw message goes to everyone | Example: /broadcast -event Double XP weekend begins now | File: BroadcastCommand.h:39  
/broadcastArea [range] [-event|-imperial|-rebel] [<message>] -- Sends a system broadcast to nearby player creatures within range (first numeric arg 6-5000, default 64), with -event tagging, -imperial/-i limited to Imperial faction plus gods, -rebel/-r to Rebel faction plus gods | Example: /broadcastArea 128 -event Fireworks display starting at the Theed hangar | File: BroadcastAreaCommand.h:42  
/broadcastGalaxy [-event|-imperial|-rebel] [<message>] -- Sends a system broadcast through ChatManager::broadcastGalaxy to all players in the galaxy, with -event/-e tagged [Event], -imperial/-i restricted to Imperials, -rebel/-r restricted to Rebels | Example: /broadcastGalaxy Server maintenance begins in five minutes | File: BroadcastGalaxyCommand.h:42  
/broadcastPlanet [-event|-imperial|-rebel] [<message>] -- Iterates the chat player map and sends a system broadcast to every player on the caller's planet, with -event/-e tagged [Event], -imperial/-i Imperial-only (plus gods), -rebel/-r Rebel-only (plus gods) | Example: /broadcastPlanet -rebel Rebel forces regroup at the Corellia base | File: BroadcastPlanetCommand.h:46  
/cityInfo <planet> [rank] -- Sends the calling admin CityManager::sendCityReport listing cities on the named planet, optionally filtered to city rank 1-5 parsed into a byte | Example: /cityInfo tatooine 3 | File: CityInfoCommand.h:30  
/database <playerstructures|cityregions|factionstructures|sceneobjects|clientobjects|resourcespawns|characters|deleted_characters> <objectid> -- Reads the raw serialized record for the given object ID from the chosen ObjectDatabase (or the characters SQL table/API) and system-messages the result to the caller | Example: /database sceneobjects 123456789 | File: DatabaseCommand.h:36  
/getGameTime [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without reporting anything | Example: /getGameTime | File: GetGameTimeCommand.h:16  
/lag [no args] -- Stub command that only runs state/locomotion checks and returns SUCCESS without parsing arguments or acting | Example: /lag | File: LagCommand.h:16  
/listGuilds <filter> -- Sends the calling player a SUI guild list of up to 100 guilds whose name or abbreviation contains the argument string (empty arguments are rejected) | Example: /listGuilds rebel | File: ListGuildsCommand.h:33  
/reconnectToTransferServer [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /reconnectToTransferServer | File: ReconnectToTransferServerCommand.h:16  
/remote [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /remote | File: RemoteCommand.h:16  
/resendLoginMessageToAll [no args] -- Resends the login message to every online player through PlayerManager | Example: /resendLoginMessageToAll | File: ResendLoginMessageToAllCommand.h:16  
/server <subcommand> [params] -- Tokenizes the first word as a registered subcommand (weather, vendorinfo, revision, info, playermanager, playerinfo, debug, market, statistics, pathfind, spawnpointinarea, who, loot) and forwards the rest of the arguments plus target to its handler | Example: /server who | File: ServerCommand.h:59  
/setLoginMessage [no args] -- Stub that only validates caller state and succeeds without parsing arguments or changing anything | Example: /setLoginMessage | File: SetLoginMessageCommand.h:16  
/setLoginTitle [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setLoginTitle | File: SetLoginTitleCommand.h:16  

## Player Inspection
/aiIgnore [no args] -- Empty stub that only performs state-mask and locomotion checks and returns success without parsing anything | Example: /aiIgnore | File: AiIgnoreCommand.h:16  
/dumpTargetInformation [no args] -- Builds a report of the caller's current target's world position, direction quaternion, cell and root building template, plus AI home/patrol points and weapon IDs, sent to the caller as a system message and in-game mail | Example: /dumpTargetInformation | File: DumpTargetInformationCommand.h:18  
/dumpZoneInformation [no args] -- Collects diagnostic info about the caller's ground zone (position, collision Z, building/cell, city count, terrain height-cache stats, up to 10 active areas) or space zone (parent ship and up to 21 in-range objects) and delivers it via system message, SUI box, and mail to the caller | Example: /dumpZoneInformation | File: DumpZoneInformationCommand.h:27  
/findObject <filter> [range] -- Searches objects within range (default twice zone width, or double space-station range in space) around the caller for displayed names containing the case-insensitive filter ("-p" matches players only) and lists them in a SUI listbox with storeable waypoints, while no arguments instead creates a direct waypoint to the caller's current target | Example: /findObject -p 128 | File: FindObjectCommand.h:79  
/findPlayer [<filter>] -- Shows the calling admin a SUI listbox of every online player whose displayed name contains the optional filter substring (all players if omitted) with waypoint/go buttons | Example: /findPlayer luke | File: FindPlayerCommand.h:42  
/getObjVars [<objectID>] -- Dumps detailed general, creature/AI, or ship information for the object with the given ID (or the current target if omitted) via system message, in-game mail, and a SUI box to the caller | Example: /getObjVars 72057594037927936 | File: GetObjVarsCommand.h:37  
/listActiveQuests [<playerName>] -- Shows a Sui list of the targeted player's active quests, falling back to a player-name token when no target is selected | Example: /listActiveQuests BobPlayer | File: ListActiveQuestsCommand.h:43  
/listCompletedQuests [<player>] -- Opens a SUI list box sent to the caller showing every quest the targeted creature or named player has completed | Example: /listCompletedQuests Bob | File: ListCompletedQuestsCommand.h:43  
/snoop [<player>] [<container>] -- Snoops the current target or named player, opening their inventory by default or the requested container/info view (equipment, datapad, bank, credits, ham, lots, vendors, veteranrewards, faction, frs, jeditrainer, buffs, modifiers, luaevents, visibility, activescreenplay, screenplaydata <screenplay> [variable], screenplaystate <stateName> [state], export [note]) | Example: /snoop JohnDoe credits | File: SnoopCommand.h:77  
/unsnoop [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without ending snooping | Example: /unsnoop | File: UnsnoopCommand.h:16  
/showFactionInformation [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /showFactionInformation | File: ShowFactionInformationCommand.h:16  
/showSpawnRegion [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /showSpawnRegion | File: ShowSpawnRegionCommand.h:16  
/secretSpawnSpam [no args] -- Stub that only validates caller state and returns success without parsing arguments or doing anything | Example: /secretSpawnSpam | File: SecretSpawnSpamCommand.h:16  
/spawnStatus [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /spawnStatus | File: SpawnStatusCommand.h:19  
/hasVeteranReward [no args] -- Stub command that only runs state/locomotion checks and returns SUCCESS without parsing arguments or acting | Example: /hasVeteranReward | File: HasVeteranRewardCommand.h:16  

## Self Control
/combat [no args] -- Empty stub that only performs state-mask and locomotion checks and returns success without parsing arguments or acting on any target | Example: /combat | File: CombatCommand.h:16  
/craft [no args] -- Empty stub that only performs state-mask and locomotion checks and returns success without parsing arguments | Example: /craft | File: CraftCommand.h:16  
/harmful [no args] -- Stub command that only runs state/locomotion checks and returns SUCCESS without parsing arguments or acting | Example: /harmful | File: HarmfulCommand.h:16  
/harmless [no args] -- Stub command that only runs state/locomotion checks and returns SUCCESS without parsing arguments or acting | Example: /harmless | File: HarmlessCommand.h:16  
/invulnerable [invis] -- Toggles the caller between invulnerable (PvP flags cleared) and normal/overt status; with the "invis" token schedules a delayed invisibility event with an effect | Example: /invulnerable invis | File: InvulnerableCommand.h:38  
/setSpeed <speed> [<duration>] -- Parses a float speed token and optional int duration (default 30) then adds a buff giving the caller that speed/acceleration multiplier for duration minutes; if piloting your own ship it instead sets that ship's max speed to speed/10 | Example: /setSpeed 2.0 15 | File: SetSpeedCommand.h:36  
/setTEF [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setTEF | File: SetTEFCommand.h:16  

## Teleport
/goto <searchTerm|?> -- Searches the caller's waypoint names for the term ('?' matches all), auto-teleports on a single match or opens a Sui list to travel to the selected waypoint | Example: /goto ? | File: GotoCommand.h:35  
/planetwarp [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /planetwarp | File: PlanetwarpCommand.h:16  
/planetwarpTarget [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /planetwarpTarget | File: PlanetwarpTargetCommand.h:16  
/teleport <x> <y> [<planet>] [<z> <parentID>] -- Switches the calling creature's zone to the given x/y coordinates (defaults to current planet, computes world floor if z omitted, defaults direction to 0), storing or launching their ship instead when piloting one depending on whether the destination zone contains "space" | Example: /teleport -120.5 3522.7 corellia 12.0 | File: TeleportCommand.h:36  
/teleportTarget [<player>] [<x> <y> [<planet>] [<z> <parentID>]] -- Teleports the named player (or the currently targeted object if no name parses) to the caller's own position by default, or to explicit x/y/z coordinates on an optional planet with optional parentID | Example: /teleportTarget LukeSkywalker -1500 3200 tatooine 45.5 | File: TeleportTargetCommand.h:48  
/teleportTo <player> -- Switches the calling creature into the named player's zone at that player's exact position and parent container, refusing self-teleports and players not in a loaded zone | Example: /teleportTo WedgeAntilles | File: TeleportToCommand.h:30  

## Stats & Economy
/gmForceCommand [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without parsing arguments or forcing anything | Example: /gmForceCommand | File: GmForceCommandCommand.h:16  
/adjustLotCount <lots> -- Adds the parsed integer amount to the maximum lots of the targeted player creature's ghost | Example: /adjustLotCount 10 | File: AdjustLotCountCommand.h:45  
/credits [<player>] <add|subtract> <amount> <cash|bank> -- Adds or subtracts the integer amount of credits from the cash or bank reserves of the targeted player creature or, if no player target, the named player looked up by first name | Example: /credits JohnDoe add 5000 bank | File: CreditsCommand.h:40  
/editBank [no args] -- Empty stub that validates state and locomotion then returns SUCCESS without parsing arguments or performing any effect | Example: /editBank | File: EditBankCommand.h:16  
/editBankAccount [no args] -- Empty stub that validates state and locomotion then returns SUCCESS without parsing arguments or performing any effect | Example: /editBankAccount | File: EditBankAccountCommand.h:16  
/editStats [no args] -- Empty stub that validates state and locomotion then returns SUCCESS without parsing arguments or performing any effect | Example: /editStats | File: EditStatsCommand.h:16  
/maxCombatAbility [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /maxCombatAbility | File: MaxCombatAbilityCommand.h:16  
/maxStats [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /maxStats | File: MaxStatsCommand.h:16  
/money [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /money | File: MoneyCommand.h:16  
/setExperience [<player>] <experienceType> <amount> -- Sets the given XP type total of the targeted player (or a player looked up by first-name token when no valid target) by awarding the difference between current and requested amount | Example: /setExperience John combat_general 50000 | File: SetExperienceCommand.h:43  
/setFaction [neutral|imperial|rebel|hutt] [onleave|covert|overt] [rank] -- Dumps the targeted tangible object's PvP/options/faction/status/rank flags when given no arguments, otherwise applies the chosen faction (neutral forcing ONLEAVE and rank 0), status, and clamped rank 0-15 to it or to a ship's pilot | Example: /setFaction rebel overt 5 | File: SetFactionCommand.h:87  
/setFactionStanding <faction> <amount> -- Sets the targeted player's standing with a valid faction to an integer between -5000 and 100000 by increasing or decreasing the difference | Example: /setFactionStanding rebel 5000 | File: SetFactionStandingCommand.h:57  
/setPlayerState [<player>] <state> -- Reads a state-name token (and a player-name token first if there is no valid player-creature target) and toggles that CreatureState on the target or named player, clearing it if present and setting it otherwise | Example: /setPlayerState Bob glowingStrong | File: SetPlayerStateCommand.h:36  
/setPublicState [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setPublicState | File: SetPublicStateCommand.h:16  

## Skills, Badges & Jedi
/gmForceRank [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without parsing arguments or changing ranks | Example: /gmForceRank | File: GmForceRankCommand.h:16  
/gmJediState <jediState> -- Sets the integer Jedi state on the current target player's PlayerObject | Example: /gmJediState 2 | File: GmJediStateCommand.h:41  
/grantBadge [-area [range]|-multiple] <badge> [<endBadge>] -- Awards the badge id (or id span with -multiple) to the targeted player, or with -area to every other player within an optional 1-192m range (default 64) | Example: /grantBadge -area 32 5 | File: GrantBadgeCommand.h:43  
/revokeBadge <badgeId> -- Parses a positive integer badge ID and revokes that badge from the ghost of the currently targeted player, notifying both parties | Example: /revokeBadge 25 | File: RevokeBadgeCommand.h:41  
/grantPadawanTrialsEligibility [no args] -- Completes the hologrind (grants Padawan trials eligibility) for the targeted creature | Example: /grantPadawanTrialsEligibility | File: GrantPadawanTrialsEligibilityCommand.h:18  
/overridePadawanTrialsEligibility [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /overridePadawanTrialsEligibility | File: OverridePadawanTrialsEligibilityCommand.h:16  
/grantSkill <skillName> -- Surrenders then awards the whole-argument skill name to the targeted creature and notifies caller and target of success or failure | Example: /grantSkill combat_marksman_master | File: GrantSkillCommand.h:36  
/revokeSkill <skill> -- Takes the whole arguments string as a skill name and makes the targeted player surrender that skill via SkillManager if they have it | Example: /revokeSkill force_title_jedi_rank_02 | File: RevokeSkillCommand.h:32  
/grantTitle [no args] -- Stub command that only runs state/locomotion checks and returns SUCCESS without parsing arguments or acting | Example: /grantTitle | File: GrantTitleCommand.h:16  
/resetJedi [no args] -- Surrenders every "force_" skill from the targeted creature and resets its Jedi state to 0 | Example: /resetJedi | File: ResetJediCommand.h:18  
/skill [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /skill | File: SkillCommand.h:16  
/setHue <hue> -- Applies the parsed integer hue to the targeted monster AI agent | Example: /setHue 31 | File: SetHueCommand.h:41  
/editAppearance [no args] -- Empty stub that validates state and locomotion then returns SUCCESS without parsing arguments or performing any effect | Example: /editAppearance | File: EditAppearanceCommand.h:16  
/setPlayerAppearance [<player>] [<template>] -- With no current target reads a player name token and looks the player up, then sets that player's alternate appearance to the next token (must contain object/mobile/shared_*.iff, verified against TemplateManager) or resets to the default template when omitted | Example: /setPlayerAppearance JohnDoe object/mobile/shared_darth_vader.iff | File: SetPlayerAppearanceCommand.h:40  

## Quests & Veteran Rewards
/clearCompletedQuest <screenplaystate|quest> <screenPlayName|questName> [<state>|<activeBitmask> <completedBitmask>] -- On the targeted player creature, clears a screenplay state by ANDing it with the inverted long value, or resets PlayerQuestData for a quest given by name hash (if it contains "quest") or Integer::valueOf number with optional active/completed step bitmasks | Example: /clearCompletedQuest screenplaystate epic_quest_1 15 | File: ClearCompletedQuestCommand.h:30  
/clearVeteranReward <milestone> -- Parses the milestone int and clears that milestone's chosen veteran reward from the targeted player creature's account info across all characters on the account | Example: /clearVeteranReward 90 | File: ClearVeteranRewardCommand.h:33  
/completeQuest <screenplaystate|quest> <screenPlayName|questName> [<state>|<activeBitmask> <completedBitmask> <finishedFlag>] -- On the targeted player creature, sets a screenplay state by ORing the parsed long bitmask, or completes a quest by name hash (if it contains "quest") or Integer::valueOf number, optionally writing full PlayerQuestData active/completed/finished values | Example: /completeQuest screenplaystate fs_quest_stage_1 7 | File: CompleteQuestCommand.h:30  
/getVeteranRewardTimeCs [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without parsing arguments | Example: /getVeteranRewardTimeCs | File: GetVeteranRewardTimeCsCommand.h:16  
/overrideActiveMonths [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /overrideActiveMonths | File: OverrideActiveMonthsCommand.h:16  
/setVeteranReward [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setVeteranReward | File: SetVeteranRewardCommand.h:16  

## Village (FS)
/gmFsVillage [no args] -- When the server's Jedi progression is village-based, calls the Lua VillageGmSui:showMainPage function for the caller, opening the FS Village GM SUI page | Example: /gmFsVillage | File: GmFsVillageCommand.h:19  

## Objects & Spawning
/createCreature <template> [objectTemplate|baby|event [level] [scale]] [X] [Z] [Y] [planet] [cellID] -- Spawns an AiAgent from the hashed template near the caller, where space zones route to ship spawning, "baby" spawns as baby, "event" spawns an event mob with level (int or min-max range, capped 500) and float scale, and remaining floats/int/long override X/Z/Y position, zone index, and parent cell ID | Example: /createCreature womprat baby | File: CreateCreatureCommand.h:49  
/createMissionElement [no args] -- Requires a privileged player ghost and forwards the raw arguments string unmodified to MissionManager::createSpawnPoint to create and save a mission NPC spawn point at the caller's outdoor position | Example: /createMissionElement | File: CreateMissionElementCommand.h:18  
/createNPC <tools|toggledebug|debugregions|shipfollow|locatespawners> -- Staff utility dispatching a lowercase subcommand: opens the staff tools SUI, toggles AI debug on the targeted AiAgent/ShipAiAgent, toggles region debug for the caller, makes the targeted ship AI escort the caller's ship, or reveals and lists nearby spawn egg spawners | Example: /createNPC toggledebug | File: CreateNPCCommand.h:39  
/createSpawningElement <lair|spawn|delete|deleteall> [<template|oid>] [level] [x z y heading] -- Spawns a lair (clamped level 1-300) or an object/structure template at the caller's or given coordinates registering it as an event object, or deletes the event object by oid or all event objects on the planet | Example: /createSpawningElement spawn object/tangible/campfire.iff 100 0 -200 45 | File: CreateSpawningElementCommand.h:51  
/createSpawningElementWithDifficulty [<closestlambda|closestlambdanotroops|closestcontainmentteam|removeclosestlambda|removeclosestcontainmentteam|closestreinforcement|probot>] -- Privileged test command that schedules an imperial Lambda shuttle containment/scan landing at the caller's position by default, or at the nearest matching GCW spawn point (optionally removing it, reporting the closest reinforcement with a waypoint, or starting a wild contraband probot scan session) | Example: /createSpawningElementWithDifficulty closestcontainmentteam | File: CreateSpawningElementWithDifficultyCommand.h:52  
/destroy [no args] -- Explicit no-op command that parses no arguments, performs nothing, and always returns SUCCESS | Example: /destroy | File: DestroyCommand.h:16  
/destroyTarget [no args] -- Combat queue command that executes doCombatAction against the caller's current target using the command's configured combat data, ignoring any argument string | Example: /destroyTarget | File: DestroyTargetCommand.h:17  
/disableTarget [no args] -- Combat queue command that executes doCombatAction against the caller's current target using the command's configured combat data, ignoring any argument string | Example: /disableTarget | File: DisableTargetCommand.h:17  
/setName [<objectID>] <name> -- Renames the targeted non-player object (or the object with the given ID read as a long token) to the remaining-string name, refusing player creatures and applying the name filter unless the caller is privileged | Example: /setName 72057594039056412 R2 Unit Salvage Droid | File: SetNameCommand.h:28  

## Resources
/gmCreateClassResource <resourceType> [<attribute>,<value>] -- Passes the raw arguments to ResourceManager::createResourceSpawn, spawning a new resource of the given resource-tree class (with any comma-separated attribute,value pairs clamped 1-1000 applied) into the manual pool and confirming to the caller | Example: /gmCreateClassResource resource/minerals/metal/copper 700,900 | File: GmCreateClassResourceCommand.h:27  
/gmCreateSpecificResource <resourceType> [<attribute>,<value>] -- Identically forwards the raw arguments to ResourceManager::createResourceSpawn, spawning a new resource of the given type with optional attribute,value overrides into the manual pool and messaging the caller | Example: /gmCreateSpecificResource resource/minerals/metal/aluminum 500,850 | File: GmCreateSpecificResourceCommand.h:30  
/resource <option> [params] -- Resource-manager admin tool for the caller dispatching subcommands list <planet>, health, dump, despawn <name>, info <name>, find <class> <attribute> <gt|lt> <value>, create <name> [quantity] | Example: /resource create steel 500 | File: ResourceCommand.h:36  
/resourceSetName [no args] -- Stub that only validates caller state and succeeds without parsing any arguments or performing any action | Example: /resourceSetName | File: ResourceSetNameCommand.h:16  

## Spawner Control
/getSpawnDelays [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without parsing arguments | Example: /getSpawnDelays | File: GetSpawnDelaysCommand.h:16  
/setMaximumSpawnTime [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setMaximumSpawnTime | File: SetMaximumSpawnTimeCommand.h:16  
/setMinimumSpawnTime [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setMinimumSpawnTime | File: SetMinimumSpawnTimeCommand.h:16  
/setPlanetLimit [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setPlanetLimit | File: SetPlanetLimitCommand.h:16  
/startCitySpawner [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /startCitySpawner | File: StartCitySpawnerCommand.h:16  
/startSpawner [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /startSpawner | File: StartSpawnerCommand.h:16  
/startTargetSpawner [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /startTargetSpawner | File: StartTargetSpawnerCommand.h:16  
/stopCitySpawner [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /stopCitySpawner | File: StopCitySpawnerCommand.h:16  
/stopSpawner [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /stopSpawner | File: StopSpawnerCommand.h:16  
/stopTargetSpawner [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /stopTargetSpawner | File: StopTargetSpawnerCommand.h:16  

## Debug / Dev Tools
/forceCommand [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without parsing arguments or affecting any target | Example: /forceCommand | File: ForceCommandCommand.h:16  
/generateCraftedItem <draftSchematicPath> [quantity] [quality] [templateIndex] -- Crafts a great-success prototype from the given server draft_schematic path into the caller's inventory (factory crate if quantity > 1), applying quality as attribute percentage (0-100) and optionally selecting the template at templateIndex | Example: /generateCraftedItem weapon/blaster_laser_pistol 5 100 0 | File: GenerateCraftedItemCommand.h:55  
/getRank [no args] -- Stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without parsing arguments | Example: /getRank | File: GetRankCommand.h:16  
/initializeComponent [no args] -- Stub command that only runs state/locomotion checks and returns SUCCESS without parsing arguments or acting | Example: /initializeComponent | File: InitializeComponentCommand.h:16  
/kill [-area [range]] [<health> [action] [mind]] -- Deals lethal-by-default HAM damage (customizable amounts) to the targeted non-player creature or lair, or to all attackable non-player creatures in an optional radius; in space zones destroys the targeted AI ship or AI ships in range | Example: /kill -area 64 | File: KillCommand.h:69  
/manufacture [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /manufacture | File: ManufactureCommand.h:16  
/npc [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /npc | File: NpcCommand.h:16  
/objvar [no args] -- No-op placeholder command that only runs state-mask and locomotion checks and returns SUCCESS with no effect | Example: /objvar | File: ObjvarCommand.h:16  
/script <option> [params] -- Lua/director debug tool for the caller dispatching subcommands readshareddata <key>, readstringshareddata <key>, getqueststatus <key>, clearallquests, setpilottier <tier>, setsquadron <squadronType>, setqueststatus <key> <value>, removequeststatus <key>, luaevents <oid> | Example: /script setpilottier 3 | File: ScriptCommand.h:31  
/searchCorpse [no args] -- Stub that only validates caller state and returns success; actual corpse searching is handled elsewhere | Example: /searchCorpse | File: SearchCorpseCommand.h:16  
/setRank [no args] -- Empty stub that only runs state-mask and locomotion checks on the caller and returns SUCCESS without any effect | Example: /setRank | File: SetRankCommand.h:16  
/startTraceLogging [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /startTraceLogging | File: StartTraceLoggingCommand.h:16  
/stopTraceLogging [no args] -- Empty stub queue command that only performs state and locomotion checks then returns SUCCESS without any effect | Example: /stopTraceLogging | File: StopTraceLoggingCommand.h:16  
/object (<createitem> <objectTemplate> [<quantity>] [<component>...] | <createloot> <lootGroup> [<level>] | <createresource> <resourceName> [<quantity>] | <createarealoot> <lootGroup> [<range>] [<level>] | <checklooted> | <characterbuilder>) -- Admin spawner that creates a tangible template item with serial/crafter name plus optional quantity and visible components into the caller's inventory, rolls loot-group items into the caller's inventory or into all players within range (default 32, clamped 0-128), grants resources, reports legendary/exceptional/magical loot counters, or spawns a character-builder terminal at the caller's position | Example: /object createitem object/tangible/wearables/armor/armor_s01_bicep_l.iff 5 | File: ObjectCommand.h:35  
/gmRevive [buff|skillmod <skillMod>|modifiers|area [<range>] [<faction>]|<name>] -- Fully restores (or with "buff" enhances, "skillmod" removes a skillmod from, "modifiers" clears buffs/resets HAM of) the current target, self, a named player, or all players/pets plus ships in an optional faction-filtered radius | Example: /gmRevive buff area 50 imperial | File: GmReviveCommand.h:60  

## Other

## Module note
Custom Skills adds **no** admin commands. `/customskills` is a normal ability-backed command. The badge/skill commands above (`/grantBadge`, `/grantSkill`, ...) are what operators use to set up what the module reads from `config.lua`. Badge IDs are **numeric**, not `config.lua` keys — map via `BadgeList` or `/requestBadges`.

## Worked scenarios
**Grant a full badge set:** find numeric IDs for your configured badges → target player → `/grantBadge -area 64 <badge id>` or repeated single grants → verify with `/requestBadges` → open `/customskills` in-game (Bonuses page shows owned count + basis-point total, capped at `cap`).  
**Undo a grant:** revoke via badge ID, re-verify with `/requestBadges`.  
**Audit:** every call is in `adminLog`; "did nothing" ⇒ check adminLog for `insufficient_permissions` (missing god mode or ability).

## Verification (R6.10)
Admin inventory extracted from the authoritative server command table (`command_command_table`, `sql/datatables.sql`; rows with characterAbility='admin' — 133 on this build). Every command entry above was parsed from its C++ class in `src/server/zone/objects/creature/commands/<Name>Command.h` by a delegated worker (opencode/x-preview-f-free), with orchestrator spot-checks against source (AdjustLotCountCommand.h:45, CityInfoCommand.h:30-37, ClearVeteranRewardCommand.h:33, CreateNPCCommand.h:39-61, SetSpeedCommand.h:27-89 incl. its built-in SYNTAX string). Stub/no-op classes are documented as stubs rather than omitted, so operators know which listed commands do nothing on this build. Gating chain + audit facts: `swgemu/CODE_REFERENCE.md`. Commands V2 PDF used as cross-reference; PDF-vs-code divergences (arg shapes, command presence) defer to code.
*Reconciled 08252026 by hy3-free (opencode/hy3-free) — BRIEF-018 redo. Contributors: hy3-free, 08242026–08252026.*
